{
  "name": "smart-food-cycle",
  "version": "1.0.0",
  "private": true,
  "description": "Smart Food Cycle - a modular full-stack platform connecting surplus food donations with beneficiaries.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"green,cyan\" \"npm run start --prefix backend\" \"npm run start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
